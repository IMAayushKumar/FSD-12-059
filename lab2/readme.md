# File System(FS Module)
    FS Module directley communicate to oprating system rather than browser the common opration on the file or folder .
    #   File
    1-Write File
    2-Read File
    3-Append File
    #   Folder
    1-mkdir/md
    2-remdir/rm
    3-readdir
    #     File metadat
    1-stat
    2-lstat
    3-rstat
    #   Watch-watch/Unwatch
    #Stream
    All functions are promise so it must be called with await keyword.


# CRUD(Create,Reterieve,Update,Delete)
    Assume we are making a cart related project.
    1.user can add Product (ID, name, price, quantity) into cart.
    2.User can see all the items of the cart.
    3.user can remove items from cart.
    4.user can also update quantity of product.
    5.All the items should be stored after a termination of project.   

